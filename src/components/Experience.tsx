import { useRef } from 'react';
import { motion, useScroll, useSpring, useReducedMotion } from 'framer-motion';
const roles=[{label:'RESEARCH & DEVELOPMENT',title:'Chief Technical Officer',org:'Vernovate Pvt. Ltd.',body:'Leading applied R&D initiatives, moving ideas from research into engineering, and procuring a ₹5 Lakhs research grant.',tags:'STRATEGY / APPLIED RESEARCH / TECHNICAL LEADERSHIP'},{label:'DEVELOPER COMMUNITY',title:'Technical Lead',org:'GDGoC AdtU',body:'Directing HackStone, technical workshops and cloud bootcamps. Creating spaces where developers learn by building together.',tags:'HACKATHONS / MENTORSHIP / CLOUD'},{label:'A FOUNDATION IN CURIOSITY',title:'Computer Science & Engineering',org:'Student & lifelong learner',body:'Developing a strong algorithmic core through 200+ LeetCode problems and hands-on software engineering.',tags:'ALGORITHMS / SYSTEMS / CONTINUOUS LEARNING'}];
export function Experience(){const ref=useRef<HTMLDivElement>(null);const {scrollYProgress}=useScroll({target:ref,offset:['start 75%','end 65%']});const progress=useSpring(scrollYProgress,{stiffness:100,damping:30});const reduced=useReducedMotion();return <section id="experience" className="section-shell section-pad experience"><div className="section-heading"><span className="eyebrow">03 / THE PATH SO FAR</span><h2>Always moving.<br/><span>Always becoming.</span></h2><p className="muted">Different roles. One drive to make things better.</p></div><div ref={ref} className="timeline"><motion.div className="timeline-progress" style={{scaleY:reduced?1:progress}}/>{roles.map((r,i)=><article className="timeline-item" key={r.title}><span className="timeline-node"/><span className="eyebrow">0{i+1} / {r.label}</span><h3>{r.title}</h3><div className="role-org">{r.org}</div><p>{r.body}</p><span className="mono timeline-tags">{r.tags}</span></article>)}</div></section>}
