import { useRef, useState, useEffect } from 'react';
import { ArrowUp, ArrowUpRight, Check, Copy } from 'lucide-react';
const email='hello@example.com';
export function Contact(){const [copied,setCopied]=useState(false),[error,setError]=useState(false);const timer=useRef<ReturnType<typeof setTimeout>|null>(null);useEffect(()=>()=>{if(timer.current)clearTimeout(timer.current)},[]);async function copy(){try{await navigator.clipboard.writeText(email);setCopied(true);setError(false);if(timer.current)clearTimeout(timer.current);timer.current=setTimeout(()=>setCopied(false),2500)}catch{setError(true)}}return <footer id="contact" className="section-shell contact section-pad"><div className="contact-top"><span className="eyebrow">04 / OPEN A CONVERSATION</span><span className="availability"><i className="status-dot"/> Available for consulting & advisory</span></div><h2>Something in mind?<br/><span>Let’s make it real.</span><ArrowUpRight className="contact-arrow" strokeWidth={1}/></h2><div className="contact-bottom"><div className="terminal"><div className="terminal-head"><span/><span/><span/><b className="mono">contact.sh</b></div><div className="terminal-body"><div className="mono"><span className="lime">~</span> initiate conversation<span className="terminal-caret">_</span></div><button onClick={copy} className="email-button" data-cursor="Copy">{email}{copied?<Check size={18}/>:<Copy size={18}/>}</button><div className="terminal-status" role="status">{copied?'✓ Email copied. Your move.':error?'Copy unavailable. Select the email text above.':'Demo contact address · replace before public launch'}</div>{copied&&<div className="confetti" aria-hidden="true">{Array.from({length:12},(_,i)=><i key={i} style={{'--i':i} as React.CSSProperties}/>)}</div>}</div></div><div className="socials"><span className="mono muted">ELSEWHERE / DEMO PROFILES</span>{['GitHub','LinkedIn','LeetCode'].map(s=><a key={s} href={{GitHub:'https://github.com',LinkedIn:'https://linkedin.com',LeetCode:'https://leetcode.com'}[s]} target="_blank" rel="noreferrer">{s}<ArrowUpRight size={20}/></a>)}</div></div><div className="footer-bar"><span>© {new Date().getFullYear()} Lungsom Lamnio</span><span className="mono">BUILT WITH INTENT. ALWAYS.</span><a href="#home" className="back-top">Back to top <ArrowUp size={15}/></a></div></footer>}
